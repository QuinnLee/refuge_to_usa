import { formatRefugee } from 'refugee-migration-us/helpers/format-refugee';
import { module, test } from 'qunit';

module('Unit | Helper | format refugee');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatRefugee([42]);
  assert.ok(result);
});
